[pytest]
addopts = -p no:doctest
testpaths = tests
markers =
    unit: fast isolated unit tests
    integration: integration tests with Flask app and DB
    performance: performance/SLA validation tests
